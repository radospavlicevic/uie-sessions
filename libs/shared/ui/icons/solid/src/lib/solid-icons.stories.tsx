import { Meta } from '@storybook/react';
import { XIcon, MenuIcon } from './solid-icons';

export default {
  title: 'icons/solid',
} as Meta;

export const X = () => <XIcon />;
export const Menu = () => <MenuIcon />;
